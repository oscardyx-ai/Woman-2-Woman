Drop the organizer/partner logo files here, named exactly:

- `empowerment-squared.png`
- `liberian-learning-center.png`
- `mcmaster-university.png`
- `global-affairs-canada.png`

The form (`index.html`) already references these paths. Until the files exist, the page
falls back to showing the organization names as plain text (see the `error` handler in
`js/script.js`).
