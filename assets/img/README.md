The event poster (`poster.png`) lives here and is referenced by the `.poster-hero`
section and the floating `.poster-dock` image in `index.html`. Until the file exists,
the page shows a placeholder box in its place (see the `error` handler in `js/script.js`).
